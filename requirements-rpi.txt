# Sentinel-X Edge (Raspberry Pi 3/4/5) Optimized Dependencies
# ==============================================================
# Pure Python wheels — Zero heavy C/C++ build dependencies, prevents OS freezing & OOM.
# Install: pip install --no-cache-dir -r requirements-rpi.txt

# --- Core Async Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.9.2
pydantic-settings>=2.5.2
python-multipart>=0.0.12
jinja2>=3.1.4

# --- Edge IoT, Telemetry & Network ---
paho-mqtt>=2.1.0
websockets>=13.1
httpx>=0.27.2

# --- Edge System Monitoring & Config ---
psutil>=6.0.0
pyyaml>=6.0.2

# --- Crypto & Security Utilities ---
python-dotenv>=1.0.1
